[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Next.js on Netlify (App Router + Route Handlers) is handled by the official plugin.
[[plugins]]
  package = "@netlify/plugin-nextjs"
